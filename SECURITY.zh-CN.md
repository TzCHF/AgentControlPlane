# 安全策略

> [English](SECURITY.md)

## 支持范围

当前版本是本地、单用户的开发服务。直接暴露于公共互联网以及不受信任的多租户使用，需要先落实“商业部署之前”一节列出的控制措施。

## 安全不变量

- 服务器默认绑定回环地址。
- 每个项目工作区都会被解析，并对照允许列表根目录进行检查。
- Codex 仅接收所选工作区作为其可写运行时根目录。
- 默认沙箱为 `workspace-write`。
- 除非本地所有者修改配置，否则网络访问处于禁用状态。
- 控制平面从不存储或索取 OpenAI API 密钥。
- 非回环绑定一律被拒绝；远程访问需要安全隧道或 TLS 认证网关。
- 公开健康检查会省略本地可执行文件路径和运行时细节。
- MCP 会话数量有上限，空闲会话会被回收。
- 状态文件和审计文件必须位于所有允许的工作区根目录之外。
- Codex 可执行文件会被解析为工作区根目录之外的绝对路径。
- 无法安全处理的审批请求会被拒绝。
- 任务变更和执行事件会写入只追加审计日志。
- 每个已认证端点都应用限流，并带有可配置的每窗口上限；超过上限的请求会收到 `429` 状态码和 `Retry-After` 响应头。
- 审计条目带有序号和链式哈希。设置 `AGENT_CONTROL_AUDIT_KEY` 会将链从 SHA-256 切换为 HMAC-SHA256。使用 `npm run verify:audit` 校验该链。
- JSON 响应设置 `X-Content-Type-Options: nosniff`、`Referrer-Policy: no-referrer` 和 `X-Frame-Options: DENY`。

## 商业部署之前

增加基于标准的 MCP OAuth、每用户工作区根目录、加密的机密存储、分布式限流、租户隔离的状态、签名审计导出、CSRF/SSRF 防护，以及独立的外部安全审查。

## 报告漏洞

目前请私下向仓库所有者报告漏洞。请勿创建包含漏洞利用细节或凭据的公开 issue。
