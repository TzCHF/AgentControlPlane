## Change

Describe the user-visible or protocol behavior changed by this pull request.

## Evidence

- Tests:
- Manual verification:
- Screenshots or task evidence:

## Safety checks

- [ ] Workspaces remain allowlisted and loopback networking remains enforced.
- [ ] Logs, fixtures, and commits contain no credentials or private paths.
- [ ] Provider-specific behavior stays in registry data or an isolated adapter.
- [ ] Tests use mocks, or the command clearly identifies live quota use.
- [ ] `npm run verify` passes.
- [ ] Changed user-facing prose passes `copy_lint.py` when available.

## Compatibility

List affected executors, protocols, persisted fields, and migration behavior.
